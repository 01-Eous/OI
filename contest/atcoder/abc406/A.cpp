#include<bits/extc++.h>
using namespace std;
using pii = pair<int,int>;
pii a,b;
signed main()
{
    cin >> a.first >> a.second >> b.first >> b.second;
    if (a > b)
        puts("Yes");
    else
        puts("No");
    return 0;
}